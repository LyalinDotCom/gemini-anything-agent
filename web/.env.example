# Used ONLY by vitest live tests and spike scripts (Node-side).
# Deliberately NOT VITE_-prefixed so Vite can never bundle it into the app.
# The app itself takes the key from the user at runtime (KeyGate -> localStorage).
GEMINI_API_KEY=
